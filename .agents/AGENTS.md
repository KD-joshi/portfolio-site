# Auto-Push Changes
After completing any requested modifications to the codebase in a Git repository, always run `git add`, `git commit` with a descriptive message, and `git push` to save and push the changes, unless explicitly instructed otherwise.
